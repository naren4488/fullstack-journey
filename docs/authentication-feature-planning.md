# Authentication — practice projects

## Purpose

Supports `slug: 'authentication'`. See [practice-projects-overview](./practice-projects-overview.md).

## Data

- **`authenticationProjects`** in [`src/data/practice/extraPart3.ts`](../src/data/practice/extraPart3.ts).

## Current projects

| Slug | Focus |
| ---- | ----- |
| `mock-login-flow` | session shape, `sessionStorage`, logout |
| `role-guard-sketch` | client-side route guard (with security caveat) |

## Routes

- `/modules/authentication` and `/modules/authentication/projects/:projectSlug`

## Note

Client-only demos are for UX learning; real authorization must be enforced on the server.
